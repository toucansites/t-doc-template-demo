id: guide
paths:
  - docs/guide

properties:
  title:
    type: string
    required: true
  order:
    type: int
    required: true

relations:
  category:
    references: category
    type: one
    order:
      key: title
      direction: asc
